
def day5_part1(rules):
    ordeer = []
    for rule in rules:
        (a,b) = rule
        if

file_path = "../Inputs/Day3"
with open(file_path, 'r') as file:
    rules = []
    index = None
    for idx, line in enumerate(file):
        if not line:
            index = idx
            break
        rules.append(line)
    updates = []
    for line in file[index:]:


